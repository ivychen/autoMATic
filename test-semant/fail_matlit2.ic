/* Create a matrix */
// Assign to matrix literal

matrix mat(bool is_square) {
	matrix M;

	if (is_square == true) {
    // FAIL: Cannot have matrix of strings
		M = [["a", "a", "a"];["a", "a", "a"];["a", "a", "a"]];
	} else {
		// 1D matrix
		M = [[1,2,3]];
	}
	return M;
}

int main() {
    bool x;
    x = true;

    mat(x); // returns square matrix

    x = false;
    mat(x); // returns 1D matrix
}
