/* Create a matrix */
// Assign to matrix literal
int main() {
    bool is_square;
		int matrix M;
    int i;
    int j;
    int test;
		is_square = false;
    i = 1;
    j = 1;

		if (is_square == true) {
		  M = [[1,2,3];[3,2,1];[3,3,3]];
      test = 10;
      print(M[1][1]); // 2
      M[0][0] = 5;
      print(M[0][0]); // 5
		} else {
      test = 20;
      M = [[6,6];[4,4]]; // illegal
      print(M[1][1]); // 4
    }

    M = [[0,0,0]]; // illegal

    print(rows(M));
    print(cols(M));

    print(test);

    test = M[i][j];
    print(test); // 4
    // M[0][0] = M[0][1];
    // print(M[0][0]); // 3
    return 0;
}
