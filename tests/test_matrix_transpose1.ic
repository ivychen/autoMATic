int main() {
    int matrix [3][3] x = [[ 5, 4, 3 ]; [ 4, 0, 4 ]; [ 7, 10, 3 ]];
    int matrix [3][3] y = x';
    int i;
    int j;

    for (i = 0; i < rows(x); i++) {
        for (j = 0; j < rows(x); j++) {
            if (x[i][j] != y[j][i]) {
                print("failure");
                print(i);
                print(j);

                return 1;
            }
        }
    }

    print("success");
    return 0;
}
